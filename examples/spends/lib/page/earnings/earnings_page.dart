import 'package:flutter_control/core.dart';

class EarningsPage extends ControlWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('earnings'),
      ),
    );
  }
}
