import 'package:flutter_control/core.dart';

class InitPage extends BaseControlWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.green,
      ),
    );
  }
}
