import 'package:flutter_control/core.dart';

class AccountPage extends ControlWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('account'),
      ),
    );
  }
}
