name: localino
description: A starting point for Dart libraries or applications.
version: 0.9.0

environment:
  flutter: '>=3.0.0'
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  control_core:
    path: ../control_core

  control_config:
    path: ../control_config

dev_dependencies:
  flutter_test:
    sdk: flutter
