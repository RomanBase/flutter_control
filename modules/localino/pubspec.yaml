name: localino
description: Json based localization.
homepage: https://localino.app
repository: https://github.com/romanbase/flutter_control
version: 1.3.0-beta.1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # https://pub.dev/packages/control_core
  control_core: ">=2.0.0-beta.2 <3.0.0"

  # https://pub.dev/packages/control_config
  control_config: ">=1.4.0-beta.1 <2.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
