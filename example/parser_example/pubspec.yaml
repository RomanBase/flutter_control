name: parser_example
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_control:
    path: ../../
  control_annotations:
    path: ../../control_annotations

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.4.9
  control_builder:
    path: ../../control_builder

flutter:
  uses-material-design: true
